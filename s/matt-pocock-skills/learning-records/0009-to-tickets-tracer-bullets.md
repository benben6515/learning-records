# `to-tickets` — tracer bullets and the frontier

The learner can distinguish a vertical tracer bullet from a horizontal layer slice, compute the active frontier from blocking edges, and identify wide refactors as expand–migrate–contract work rather than forcing them into a tracer bullet. This matters because it explains how each `implement` session can start fresh with one independently buildable ticket.

**Evidence:** correctly selected the end-to-end permission-menu behavior over schema-only or UI-only work; identified frontiers `{T1, T4}`, `{T1}`, and `{T2}` as dependencies were cleared; and recalled the three migration stages for a repo-wide rename.

**Implications:** ready to move from the spec-driven spine into `implement` and code review.
