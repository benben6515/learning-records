# Starting point &amp; mission established

Recorded at workspace creation. The user disclosed prior knowledge and pinned the mission scope — both set the floor for what to teach next.

- **Prior knowledge (claimed, comfortable):** fluent with agent harnesses and the skill-invocation model. → Future lessons skip "what is a skill / how to invoke" and go straight to workflow reasoning. No evidence demonstrated yet — this is a stated floor, to be confirmed when the user reasons through a real situation.
- **Mission scope pinned to spec-driven dev** (the middle stretch of the main flow: `grill-with-docs → to-spec → to-tickets`), with `domain-modeling` as the supporting discipline. The issue/triage pipeline, the AFK `wayfinder`/`handoff` loop, and `writing-great-skills` authoring are explicitly out of scope for now. The full main flow is taught only as a map (Lesson 1), not drilled.
- **Style preference: conceptual / reference-first.** Favour maps, reference docs, and mental models over hands-on drills. Drills are deferred to the point where the user actually ships their first spec (the `implement`/`tdd`/`code-review` consumers).

**Implications:** the zone of proximal development is "understand the system as one flow, then master the spec-driven spine conceptually." Lessons should each deliver one mental model + one self-check, not a coding exercise. The Workflow Map reference doc is the spine the user will return to.
